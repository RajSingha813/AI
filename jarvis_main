#paste below the "go to sleep" function
elif "auto" in query or "ai mood" in query:
                    speak("AI mode activated")
                    from AI import auto_writer
                    auto_writer() 
